[["// 📖 읽기 중 만난 한자를 그 자리에서 획순 연습 — chars 는 한자 1글자 배열\nfunction openWriteChar(chars, label, back){\n  const I=rdBuildIndex();", "// 📖 읽기 중 만난 한자를 그 자리에서 획순 연습 — chars 는 한자 1글자 배열\nfunction openWriteChar(chars, label, back, start){\n  const I=rdBuildIndex();"], ["  if(!list.length){ toast(\"이 단어엔 한자가 없어요\"); return; }\n  wrAdhoc=list; wrAdhocLabel=label||\"이 단어\"; wrCat=\"_adhoc\"; wrIdx=0;\n  wrBack=back||\"rdRead\"; wrOrderMode=2;                 // 책에서 왔으면 획순을 처음부터 보여 준다", "  if(!list.length){ toast(\"이 단어엔 한자가 없어요\"); return; }\n  wrAdhoc=list; wrAdhocLabel=label||\"이 단어\"; wrCat=\"_adhoc\";\n  wrIdx=Math.max(0, Math.min(list.length-1, start|0));   // 누른 글자부터 시작, ←→ 로 나머지도\n  wrBack=back||\"rdRead\"; wrOrderMode=2;                 // 책에서 왔으면 획순을 처음부터 보여 준다"], ["    const lg=wrLog();\n    kbox.innerHTML = `<div class=\"lb\">✍️ 한자를 누르면 획순을 따라 써볼 수 있어요</div><div class=\"row\">`", "    const lg=wrLog();\n    const wq=String(w||\"\").replace(/'/g,\"\");\n    kbox.innerHTML = `<div class=\"lb\">✍️ 한자를 누르면 획순을 따라 써볼 수 있어요</div><div class=\"row\">`"], ["    kbox.innerHTML = `<div class=\"lb\">✍️ 한자를 누르면 획순을 따라 써볼 수 있어요</div><div class=\"row\">`\n      + kj.map(ch=>{ const n=lg[\"book|\"+ch]|0;\n          return `<button onclick=\"openWriteChar('${ch}','${(w||\"\").replace(/'/g,\"\")}','rdRead')\">`\n               + `<span class=\"ch\">${ch}</span><span class=\"tg\">${n?n+\"번 씀\":\"따라쓰기\"}</span></button>`;", "    kbox.innerHTML = `<div class=\"lb\">✍️ 한자를 누르면 획순을 따라 써볼 수 있어요</div><div class=\"row\">`\n      + kj.map((ch,i)=>{ const n=lg[\"book|\"+ch]|0;\n          return `<button onclick=\"openWriteChar('${wq}','${wq}','rdRead',${i})\">`\n               + `<span class=\"ch\">${ch}</span><span class=\"tg\">${n?n+\"번 씀\":\"따라쓰기\"}</span></button>`;"]]