[["  const done = rdChapCur+1 >= bk.chapters.length;\n  rdStopPlay();\n  if(rdCont && done) toast(\"📖 책을 끝까지 다 읽었어요 🎊\");\n}","  const done = rdChapCur+1 >= bk.chapters.length;\n  rdStopPlay();\n  rdToTop();                      // 다 읽었으면 맨 위로 — 다시 읽거나 시험 보러 가기 편하게\n  if(rdCont && done) toast(\"📖 책을 끝까지 다 읽었어요 🎊\");\n}\n// 읽기 끝 → 본문 맨 위로. 페이지 스크롤과 본문 스크롤 양쪽을 다 올린다\n//  (기기·화면에 따라 스크롤 주체가 달라서 한쪽만 올리면 그대로 남아 있는 경우가 있다)\nfunction rdToTop(){\n  try{ window.scrollTo({top:0,behavior:\"smooth\"}); }catch(e){ window.scrollTo(0,0); }\n  const b=$(\"rrBody\"); if(b && b.scrollTop) { try{ b.scrollTo({top:0,behavior:\"smooth\"}); }catch(e){ b.scrollTop=0; } }\n  const sc=document.querySelector(\"#rdRead\"); if(sc && sc.scrollTop){ try{ sc.scrollTo({top:0,behavior:\"smooth\"}); }catch(e){ sc.scrollTop=0; } }\n}"],[".rd-filter{display:grid;grid-template-columns:repeat(6,1fr);gap:5px;padding:12px 16px 2px}",".rd-filter{display:grid;grid-template-columns:repeat(6,1fr);gap:5px;padding:12px 16px 2px}\n/* v5.7 — 104편이라 단계 필터만으로는 못 고른다. 검색 + 정렬을 한 줄에. */\n.rd-sort{display:flex;gap:6px;padding:8px 16px 2px;align-items:center}\n.rd-sort input{flex:1;min-width:0;padding:9px 12px;border:1px solid var(--line);border-radius:12px;\n  background:var(--card);font-size:13px;color:var(--ink);outline:none}\n.rd-sort input:focus{border-color:var(--accent)}\n.rd-sort button{flex:0 0 auto;padding:8px 10px;border:1px solid var(--line);background:var(--card);\n  border-radius:12px;font-size:12px;font-weight:800;color:var(--sub);white-space:nowrap}\n.rd-sort button.on{background:var(--ink);color:#fff;border-color:var(--ink)}"],["  <div class=\"rd-filter\" id=\"rdFilter\"></div>","  <div class=\"rd-filter\" id=\"rdFilter\"></div>\n  <div class=\"rd-sort\" id=\"rdSort\"></div>"],["  el.textContent = d ? (t? `${d}/${t}장` : `${d}장 읽음`) : \"5세부터\";","  // 아직 안 읽었으면 \"몇 편 있는지\"를 보여 준다 — 이게 가장 강한 가치 증명이다.\n  //   (예전엔 \"5세부터\"라 규모를 전혀 알 수 없었다)\n  el.textContent = d ? (t? `${d}/${t}장` : `${d}장 읽음`)\n                     : (BOOKS ? `원서 ${BOOKS.length}편` : \"5세부터\");"],["  renderRdList();\n  renderRdBar(); renderRdResume();\n}\nfunction rdSetLv(v){\n  rdLvFilter=v;\n  document.querySelectorAll(\"#rdFilter button\").forEach((b,i)=>b.classList.toggle(\"on\", i===v));\n  renderRdList();\n}","  renderRdSort();\n  renderRdList();\n  renderRdBar(); renderRdResume();\n}\nfunction rdSetLv(v){\n  rdLvFilter=v;\n  document.querySelectorAll(\"#rdFilter button\").forEach((b,i)=>b.classList.toggle(\"on\", i===v));\n  renderRdList();\n}\n// ── v5.7 작품이 100편을 넘으면서 \"고를 방법\"이 필요해졌다 ──\n//   단계 필터만으로는 20편이 넘게 남아 스크롤로 훑어야 한다. 검색 + 정렬을 붙인다.\nlet rdQuery=\"\", rdSort=\"short\";     // short=짧은 순(첫 원서용) · lv=단계순 · long=긴 순\nfunction renderRdSort(){\n  const el=$(\"rdSort\"); if(!el) return;\n  el.innerHTML =\n    `<input id=\"rdQ\" type=\"search\" placeholder=\"제목·작가 검색\" value=\"${rdQuery.replace(/\"/g,'&quot;')}\"\n       oninput=\"rdSetQuery(this.value)\">`\n    + [[\"short\",\"짧은 순\"],[\"lv\",\"단계순\"],[\"long\",\"긴 순\"]]\n      .map(([v,t])=>`<button class=\"${v===rdSort?'on':''}\" onclick=\"rdSetSort('${v}')\">${t}</button>`).join(\"\");\n}\nlet _rdQT=null;\nfunction rdSetQuery(v){\n  rdQuery=v.trim();\n  clearTimeout(_rdQT); _rdQT=setTimeout(()=>renderRdList(),160);   // 타이핑 중 매 글자 렌더 방지\n}\nfunction rdSetSort(v){\n  rdSort=v;\n  document.querySelectorAll(\"#rdSort button\").forEach(b=>b.classList.toggle(\"on\", b.textContent===({short:\"짧은 순\",lv:\"단계순\",long:\"긴 순\"})[v]));\n  renderRdList();\n}\nfunction rdSents(b){ return b.chapters.reduce((a,c)=>a+c.s.length,0); }"],["  const idx=bs.map((b,i)=>[b,i]).filter(([b])=> !rdLvFilter || b.lv===rdLvFilter);","  const q=rdQuery.toLowerCase();\n  let idx=bs.map((b,i)=>[b,i]).filter(([b])=> (!rdLvFilter || b.lv===rdLvFilter) &&\n    (!q || [b.title_ko,b.title,b.author_ko,b.author,b.intro_ko].filter(Boolean)\n             .some(t=>String(t).toLowerCase().includes(q))));\n  idx.sort((A,B)=> rdSort===\"lv\" ? (A[0].lv-B[0].lv || rdSents(A[0])-rdSents(B[0]))\n                 : rdSort===\"long\" ? rdSents(B[0])-rdSents(A[0])\n                 : rdSents(A[0])-rdSents(B[0]));"]]